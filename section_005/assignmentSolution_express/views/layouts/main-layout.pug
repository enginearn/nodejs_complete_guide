<!DOCTYPE html>
html(lang="ja")
    head
        meta(charset="utf-8")
        meta(name="viewport", content="width=device-width, initial-scale=1")
        link(rel="stylesheet", href="/css/main.css")
        title #{pageTitle}
    body
        header
            a(href="/") Home | a(href="/users") Users
    block content
