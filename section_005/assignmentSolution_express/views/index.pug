extends layouts/main-layout.pug

block content
    h1 #{pageTitle}
    h2 Welcome to the User Management App!
    form(action="/add-user", method="POST")
        input(type="text", name="username", placeholder="Enter Username")
        button(type="submit") Add User
